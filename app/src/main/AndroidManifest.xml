<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.clearkeep">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Subscriptions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        tools:replace="android:allowBackup"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoJetpackCompose"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".screen.chat.contact_search.SearchUserActivity"
            android:theme="@style/Theme.DemoJetpackCompose.NoActionBar" />
        <activity
            android:name=".screen.chat.group_create.CreateGroupActivity"
            android:theme="@style/Theme.DemoJetpackCompose.NoActionBar" />
        <activity
            android:name=".screen.chat.room.RoomActivity"
            android:theme="@style/Theme.DemoJetpackCompose.NoActionBar" />
        <activity
            android:name=".screen.splash.SplashActivity"
            android:theme="@style/Theme.DemoJetpackCompose.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screen.auth.login.LoginActivity"
            android:label="@string/btn_login"
            android:theme="@style/Theme.DemoJetpackCompose.NoActionBar" />
        <activity
            android:name=".screen.auth.register.RegisterActivity"
            android:label="@string/btn_register"
            android:theme="@style/Theme.DemoJetpackCompose.NoActionBar" />
        <activity
            android:name=".screen.chat.home.HomeActivity"
            android:theme="@style/Theme.DemoJetpackCompose.NoActionBar" />

        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".januswrapper.services.InCallForegroundService"
            android:foregroundServiceType="camera|microphone" />

        <activity
            android:name=".januswrapper.InCallActivity"
            android:theme="@style/Theme.FastGo.Call" />

        <activity
            android:name=".januswrapper.InComingCallActivity"
            android:theme="@style/Theme.FastGo.Call" />

        <!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_launcher_foreground" />
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@android:color/holo_blue_dark" />

        <uses-library android:name="org.apache.http.legacy" android:required="false"/>
    </application>

</manifest>
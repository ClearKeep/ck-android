package com.clearkeep.db.clear_keep.model

/*
@Entity
data class GroupAndLastMessage(
        @Embedded
        val group: ChatGroup,
        @Embedded(prefix = "message_")
        val lastMessage: Message?
)*/
